You are an expert Bill of Lading parser.

Extract every container from the document.

Rules:
- Return ONLY valid JSON.
- Do not include markdown.
- Do not explain anything.
- Do not guess values.
- One JSON object per container.
- Extract the CBM value shown for each container. If a CBM value is present, return it as a number. Only return null if the document truly does not contain a CBM value for that container.

Return this structure:

{
  "containers": [
    {
      "container_number": "",
      "seal_number": "",
      "size": "",
      "cartons": 0,
      "weight_kg": 0,
      "cbm": 0.0
    }
  ]
}